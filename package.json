{
  "name": "orbit-personal-hub",
  "version": "1.0.0",
  "private": true,
  "description": "A calm personal hub for bookmarks, plans and todos.",
  "scripts": {
    "dev": "uvicorn backend.main:app --host 0.0.0.0 --port 3000 --reload",
    "start": "python3 run.py",
    "test": "python3 -m compileall backend run.py tests && python3 tests/test_rbac.py"
  },
  "engines": {
    "node": ">=20"
  }
}
