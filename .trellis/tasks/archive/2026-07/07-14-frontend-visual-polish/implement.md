# 实施计划

> 状态说明（2026-07-14）：用户决定保留原版界面，本任务产生的 `public/index.html`、`public/styles.css`、`public/app.js` 改动已完整回滚到 Git `HEAD`。下列勾选项仅保留本轮尝试记录，不代表当前代码仍包含视觉重设计。

## 1. 建立安全基线

- [x] 记录 `public/index.html`、`public/styles.css`、`public/app.js` 的当前 Git 状态，避免覆盖用户改动。
- [x] 列出 `public/app.js` 依赖的 `id`、`data-*` 和状态类，作为 HTML 重构核对表。
- [x] 运行一次现有测试，记录改造前基线。

## 2. 重构页面结构与文案

- [x] 更新 `public/index.html` 的字体加载、主题色和缓存版本。
- [x] 重组登录页、侧栏、顶栏、概览及各页面标题/分区结构，同时保留交互契约。
- [x] 删除装饰性英文眉题、励志引语、轨道星球和无信息量标签。
- [x] 将内嵌展示文案改成自然、具体的中文。
- [x] 如需调整动态文案，只修改 `public/app.js` 中的展示字符串，不改事件和数据流。

## 3. 重建视觉系统

- [x] 按 token、基础元素、框架、组件、页面、响应式的顺序重写 `public/styles.css`。
- [x] 实现纸墨色彩、系统字体、细分隔线、小圆角和克制阴影。
- [x] 完成按钮、表单、筛选、列表、状态、弹窗、Toast、空状态和 PlayCaptcha 外围样式。
- [x] 确保只有分类/状态使用胶囊形，只有浮层使用明显阴影。

## 4. 完成页面级样式

- [x] 登录/注册与验证码。
- [x] 应用框架、桌面侧栏、移动抽屉和顶栏搜索。
- [x] 概览、收藏、摘录、计划、待办和网盘搜索。
- [x] Hermes 聊天、Hermes 管理、用户管理和聊天审计。
- [x] 长标题、长网址、长消息、空列表和高数据密度状态。

## 5. 响应式与可访问性

- [x] 在 1000px、760px 和 480px 附近核对布局变化，修复横向溢出。
- [x] 确保手机端主要按钮和导航具有足够触控面积。
- [x] 为链接、按钮、输入框、选择框和可拖拽项补齐一致的 `:focus-visible`。
- [x] 核对文字、边界、危险和禁用状态对比度。
- [x] 尊重 `prefers-reduced-motion`，关闭非必要动画。

## 6. 反 AI 审校

- [x] 搜索残留的大写英文眉题、星芒字符、装饰性渐变、发光阴影和过度大圆角。
- [x] 对全部展示文案执行 humanizer 反模板检查，保留准确的功能与风险含义。
- [x] 核对页面是否仍存在“所有内容都套同款卡片”的结构。

## 7. 验证

- [x] `node --check public/app.js`
- [x] `npm test`
- [x] 对比 HTML 中的交互契约与 JS 选择器，确认无缺失或重复。
- [x] 在 1440×1000 检查登录、概览、收藏/摘录、计划、Hermes 聊天、管理页和弹窗。
- [x] 在 390×844 检查登录、移动侧栏、页面标题操作、列表、表单、聊天输入和弹窗。
- [x] 检查空、有数据、hover、focus-visible、disabled、错误、成功和长文本状态。
- [x] 运行 `git diff --check`，确认没有格式错误或意外文件改动。

## 8. 风险与回滚点

- HTML 重排后立即核对选择器契约；若失败，先恢复 DOM 属性，不改 JS 业务逻辑。
- CSS 框架与页面模块分阶段检查，避免到最后才发现移动端回归。
- 临时静态预览或模拟数据只放在临时目录，不提交到仓库。
- 不触碰后端、数据库、部署配置和 PlayCaptcha 内部实现。

## 9. 用户反馈修订

- [x] 在书房气质和活泼面板之间收敛为现代书房风，不采用糖果色效率面板。
- [x] 减少主要容器和导航中的分隔线，使用色块、圆角和轻阴影建立层级。
- [x] 概况页首屏展示个性化问候与随机摘录，并支持直接切换摘录。
- [x] 移除陶土色，将按钮、当前导航、Logo 和选中态统一为摘录区的亚麻色，并以深亚麻棕保持可读对比度。
- [x] 完成桌面与 390px 手机端视觉检查。
- [x] 完成 code-spec 评估：本轮只调整展示层，没有新增可复用代码契约，无需修改 `.trellis/spec/`。
